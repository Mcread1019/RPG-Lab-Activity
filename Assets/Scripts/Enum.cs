using UnityEngine;

public class Enum : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created


    enum Charcterclass {Archer, Warrior, Barbarian, Bard };
    enum CharcterType {Human, Elf, Orc, Dwarf };

    enum AbilityScoreName {Strength, Dexterity, Constitution, Intelligence, Wisdom, Charisma };
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
